let bandNumber = 1 //variable

const takeNumber = function (bandname) {
    //Prints out the bandname and its placement
    console.log (`${bandname} is number ${bandNumber}.`);
    //Increments the bandNumber
    bandNumber++;
}

const scum = takeNumber("Galactic Scum")

const under = takeNumber("Underdogs")
